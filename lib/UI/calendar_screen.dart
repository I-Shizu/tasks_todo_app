import 'package:flutter/material.dart';

class CalendarScreen extends StatelessWidget {
  const CalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // カレンダー表示の実装（例としてプレースホルダーを使用）
    return const Center(
      child: Text('カレンダー表示'),
    );
  }
}