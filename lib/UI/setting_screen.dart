import 'package:flutter/material.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // 設定画面の実装（例としてプレースホルダーを使用）
    return const Center(
      child: Text('設定画面'),
    );
  }
}